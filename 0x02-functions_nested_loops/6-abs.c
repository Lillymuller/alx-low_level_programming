#include "main.h"
/**
  *_abs - compute the absolut value of an integer
  *@i: This is the input value integer
  *Return: Absolute value
  */
int _abs(int i)
{
if (i >= 0)
{
	return (i);
}
else
{
	return (i * -1);
}
}
