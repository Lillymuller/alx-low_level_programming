#include "main.h"
/**
  *mul - multiplies two integers
  *@a: is an integer
  *@b: is an integer
  *Return: Always 0 (success)
  */
int mul(int a, int b)
{
	int result = (a * b);
	{
		return (result);
	}
}
